'use strict';

const fs = require('fs-extra');
const recast = require('recast');

/**
 * Write a config to a given file
 * @param {string} filename
 * @param {Config} config
 * @returns void
 */
async function write(filename, config) {}

module.exports = write;
